document.addEventListener('DOMContentLoaded',async()=>{const sb=window.mccSupabase,form=document.getElementById('setup-form'),msg=document.getElementById('setup-message'),pw=document.getElementById('setup-password');document.getElementById('toggle-password').onclick=()=>{pw.type=pw.type==='password'?'text':'password'};const {data:availableData,error:availableError}=await sb.rpc('admin_setup_available');if(!availableError&&!availableData){msg.textContent='O administrador inicial já foi configurado. Use a tela de login.';form.querySelector('button[type="submit"]').disabled=true;return}form.onsubmit=async e=>{e.preventDefault();const fd=new FormData(form),email=String(fd.get('email')||'').trim(),password=String(fd.get('password')||''),name=String(fd.get('name')||'').trim();msg.textContent='Criando acesso...';const {data,error}=await sb.auth.signUp({email,password,options:{data:{full_name:name}}});if(error){msg.textContent='Não foi possível criar o acesso: '+error.message;return}if(data.session){const {error:claimError}=await sb.rpc('claim_first_admin',{p_full_name:name});if(claimError){msg.textContent='A conta foi criada, mas não foi possível concluir o administrador: '+claimError.message;return}msg.textContent='Administrador criado com sucesso. Você já pode entrar no painel.';setTimeout(()=>location.href='admin.html',1200)}else{msg.textContent='Conta criada. Verifique o e-mail para confirmar. Depois, volte à tela Admin e faça login; o primeiro acesso confirmado concluirá a configuração.'}}});